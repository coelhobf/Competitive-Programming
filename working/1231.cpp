#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a, b;
    while(cin >> a >> b)
    {
        vector<string> set1(a), set2(b);

        for(auto &x: set1)
        {
            cin >> x;
        }

        for(auto &x: set2)
        {
            cin >> x;
        }

        
    }
}